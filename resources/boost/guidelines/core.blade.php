## Revive Code Formatter

- You must run `vendor/bin/revive fix --dirty` before finalizing changes to ensure your code matches the project's expected style.
- Revive wraps Laravel Pint and other formatters, so never run Pint directly. Always prefer Revive for formatting tasks.
